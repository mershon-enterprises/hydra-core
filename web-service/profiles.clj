{:dev {
       :env {
             ; Hydra Core
             :db-host           "localhost"
             :db-port           5432
             :db-name           "postgres"
             :db-user           "postgres"
             :db-password       "password"
             :authenticator     "match"
             :rabbitmq-host     "localhost"
             :rabbitmq-username "guest"
             :rabbitmq-password "guest"
             }}}